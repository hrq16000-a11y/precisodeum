/**
 * Step01_Identity — Identificação (nome + WhatsApp) na triagem unificada.
 *
 * Re-export plano do componente que vive em `bet/PhaseIdentity.tsx`.
 * Parte da consolidação Fase 2 (achatamento da árvore de steps).
 */
export { default } from './bet/PhaseIdentity';
