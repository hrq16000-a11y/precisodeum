/**
 * Constantes compartilhadas do projeto.
 * Centraliza valores que aparecem em múltiplos arquivos.
 */

/**
 * Tamanho padrão de página para listagens no painel admin.
 * Páginas com volume naturalmente maior (ex: métricas por rota × dia)
 * podem definir constante local própria (ex: 100) — não força granularidade onde não faz sentido.
 */
export const ADMIN_PAGE_SIZE = 50;
